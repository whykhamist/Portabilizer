
namespace Configuration
{
    public enum MergePath
    {
        /// <summary>
        /// Start from the first config to the last
        /// </summary>
        Ascending,

        /// <summary>
        /// Start from the last config to the first
        /// </summary>
        Descending
    }
}
